#include "main.h"
/**
 * print_alphabet - takes no parameters
 *
 * loops from 97 to 122 which is a to z(ASCII)
 *
 * new line at the end
 */
int _islower(int c)
{
	return (c >= 97 && c <= 127 ? 1 : 0);
}
