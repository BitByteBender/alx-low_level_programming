#include <string.h>
#include "main.h"
/**
 * _strlen - Takes a char pointer as a param
 * @s: a pointer to the 1st char of a string
 * strlen(): Calculates the length of a string
 *
 * Return: The length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
