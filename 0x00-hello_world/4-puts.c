#include <stdio.h>

/**
* main - Entry point
* main is a special function.
* Declare a var named Text that gets a pointer to a constant string.
* Used puts for single line to output the string var value.
*
* Return: Always (Success).
* return value of main indicates a successful execution.
*/
int main(void)
{
const char *Text = "\"Programming is like building a multilingual puzzle";
puts(Text);
return (0);
}
